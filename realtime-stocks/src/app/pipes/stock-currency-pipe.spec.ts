import { StockCurrencyPipe } from './stock-currency-pipe';

describe('StockCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new StockCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
